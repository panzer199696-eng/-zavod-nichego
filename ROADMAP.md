# ROADMAP — «Ничего не построено»

> Дорожная карта развития игры. **Ночной авто-режим** («Ночная смена») берёт
> ПЕРВЫЙ незакрытый этап `[ ]`, прорабатывает его за ночь и отмечает `[x]`.
> Один этап за ночь. Принцип: маленькие, тестируемые, обратимые шаги.
>
> Правила для ночного прогона — в `.night/night_prompt.md`.
> Прогресс и заметки — в `.night/logs/ГГГГ-ММ-ДД.md`.

---

## ⚠️ СМЕНА ДВИЖКА: Pygame → Godot 4 (решено 2026-06-09)

Игра мигрирует на **Godot 4** (стабильный Android + Steam, нормальный визуал).
Решение и обоснование — в сессии 2026-06-09. Ключевой принцип миграции:

- **Логика (`engine/` + `game_state/`, ~1544 стр., 0 зависимостей от pygame) = СПЕЦИФИКАЦИЯ.**
  Она переносится на GDScript механически и проверяется существующими тестами
  (81 зелёный — golden-эталон по seed). Любая работа над логикой/контентом
  (карты, враги, реликвии, баланс, мета) **переносится в Godot бесплатно** → её
  МОЖНО делать ночью прямо сейчас.
- **Презентация (`scenes/` + `ui/` + `main.py`, pygame) = ВЫБРАСЫВАЕТСЯ** и пишется
  заново на узлах Godot. Любая полировка Pygame-UI = выброшенная работа →
  ночью НЕ делать (этапы помечены 🛑 PYGAME-UI ниже).

**Ночной авто-режим до установки Godot работает только над port-safe логикой/контентом
(раздел «Этапы — логика/контент»). Презентационные этапы заморожены до Godot-скелета.**

---

## Текущее состояние (база `fc34970`)
- Акт 1: меню → карта (6 узлов) → бой → награда → gameover.
- ~26 карт, 10 реликвий, 3 редкости, взвешенные награды.
- Статусы: Бюрократия, Согласовано, Заморозка, Промерзание, Запал, Простой, Пустота.
- Архетипы: Запал-бёрст / Броня-танк / Контроль-дебаф / Цикл-добор.
- 81 тест зелёный (golden-эталон). Полный авто-забег ~85% побед, стена — босс.

---

## Этапы — логика/контент (PORT-SAFE, можно ночью; по одному за ночь)

- [x] **B. Баланс мёртвых карт.** ✅ 2026-06-15 — добавлен `base`-минимум урона
  (КМ-2 base 3, Аврал base 3 cap 24→27, Переработка base 2); `test_dead_cards.py`
  проверяет ненулевой урон при 0 стаках. Коммит `0ae39c6`, 29 тестов зелёные.

- [x] **C. Расширение пула карт (+6).** ✅ 2026-06-15 — Гореть на работе (бёрст,
  unc), Круговая оборона (танк, com), Стоп-работа (контроль-заморозка, unc),
  Бумажная волокита (контроль-Простой, com), Текучка (цикл-кантрип, unc),
  Поручение (цикл, com). Все на существующих primitives. +10 тестов (99 всего),
  дубль-чек/веса наград держатся. Коммит — см. ниже.

- [x] **D. Расширение реликвий (+4).** ✅ 2026-06-15 — постоянные (каждый ход)
  пассивы: Вечный дедлайн (+1 Запал, бёрст), Каска с трещиной (+2 брони, танк),
  Журнал замечаний (Простой 1 врагу, контроль), Скоросшиватель (+1 карта, цикл).
  Добавлен `RunState.roll_relic_reward()` (дедуп по собранным, детерминизм по
  seed) — основа награды элиты/босса. +10 тестов (109 всего).

- [x] **G. Мета‑прогрессия (модель данных).** ✅ 2026-06-15 — `game_state/meta.py`:
  `MetaState` (командировочные + множество разблокировок), каталог цен (4 карты +
  4 реликвии + 3 старт-бонуса), JSON save/load (битый/нет файла → свежий профиль),
  гейтинг пула `available_card_factories/available_relic_classes`. Интеграция в
  `RunState`: opt-in `meta`, старт-бонусы (+5 HP / стартовая реликвия / 4 карты в
  награду), `bank_currency_to_meta()`. Гейтинг ОПЦИОНАЛЕН (meta=None → как раньше),
  поэтому старые тесты целы. +22 теста (131 всего). UI-лавка — потом в Godot GD-5.

- [ ] **H. Акт 2 (контент).** Новый набор врагов + босс, рост сложности, новые статусы
  по нужде — данными в `content.py`. Тесты на врагов и проходимость в симуляции.

## Этапы — миграция на Godot
> GD-0/5/6 — ручной/coder (инфра, сцены, сборка). **GD-4 — ночной headless-режим РАЗРЕШЁН**
> (нативный раннер SceneTree, golden-контракт как защитная сеть). Работа в `game-godot/`.

- [ ] **GD-0. Инфраструктура.** Установить Godot 4 (stable) + Android Build Template +
  Android SDK (JDK 21 уже есть). Создать проект-скелет 360×640, экспорт-пресеты
  Android(arm64)+Windows. Собрать ПУСТОЙ подписанный APK и Windows-сборку —
  выявить грабли до контента. **Блокер на всё остальное в Godot.**
- [x] **GD-1. Golden-исходы по seed** сняты → `game-godot/data/golden_battles.json` (48 боёв,
  4 врага × 12 seed). Генератор `gen_golden.py`. ✅ 2026-06-09
- [x] **GD-2. Порт боевого ядра** → GDScript (`game-godot/scripts/logic/`). RNG — портируемый
  SplitMix64 (Python MT19937 ≠ Godot PCG32), Python-двойник в `tools/`. Верифицировано headless:
  RNG 22/22, UNIT 16/16, **GOLDEN 48/48 бит-в-бит**. ✅ 2026-06-09
- [x] **GD-3. Контент** портирован: 21 карта (паритет), 6 врагов, 2 героя, 10 реликвий
  (`relic.gd`+хуки combat.gd), `run_state.gd` (RunState+reward_choices), `game_map.gd` (Акт 1).
  Контракт держится: RNG 22/22, UNIT 47/47, GOLDEN 48/48. ✅ 2026-06-09
- [x] **GD-4. Верификация и интеграция** ✅ 2026-06-11:
  (а) `test_unit.gd` расширен до 61 проверки (было 47): паритет статус-утечки (статусы/броня не
  переживают бой, HP переносится) + реликвии Бейдж/Ненорм.график/Толстая папка/Вторая смена;
  (б) `BattleRunner.run(enemy, seed, relics=[])` принимает и применяет реликвии забега (дефолт `[]`
  → golden не затронут), интеграц.тест: Чёрный список через runner режет урон (hp 77→80). RNG 22/22,
  UNIT 61/61, **GOLDEN 48/48** держится. Также инициализирован git (main + ветка night/auto).
- [ ] **GD-5. Презентация на узлах** (BattleScene, Tween/AnimationPlayer вместо math.sin,
  GPUParticles2D вместо spawn_burst), подключить PNG-арты + палитра `#1A1538`.
- [ ] **GD-6. Вертикальный срез Акта 1**: APK на устройстве + Steam-сборка, авто-забег как смоук.

## Этапы — презентация (🛑 PYGAME-UI, ЗАМОРОЖЕНЫ — переносятся в GD-5, ночью НЕ делать)

- [ ] 🛑 **A. Прозрачность пассива «Запал».** (Pygame-панель героя — выброс.) → делать в Godot GD-5.
- [ ] 🛑 **E. События‑развилки (сцена).** Логика выборов/последствий port-safe, но
  САМА сцена-UI — в Godot. Можно ночью сделать ТОЛЬКО модель события (данные+эффекты на RunState), без Pygame-сцены.
- [ ] 🛑 **F. Второй герой «Директор».** Колода/пассив/финишеры (логика) port-safe и
  можно ночью; выбор героя в меню (UI) — в Godot GD-5.

---

## Журнал закрытых этапов
<!-- Ночная смена дописывает сюда строку на каждый завершённый этап -->
- 2026-06-11 — **GD-4** закрыт: паритет тестов (61 проверка) + интеграция реликвий в `BattleRunner.run`; git инициализирован (main/night/auto); контракт RNG 22/UNIT 61/GOLDEN 48 зелёный.
